interface:
  display_name: "Rig Handoff"
  short_description: "Copy this session for the other coding agent"
  default_prompt: "Use $handoff to copy this session for the other coding agent to continue."
policy:
  allow_implicit_invocation: false
